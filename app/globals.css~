@import "tailwindcss";

@theme {
  --font-mont: "Montserrat Alternates", sans-serif;
  --font-weight-400: 400;
  --font-weight-600: 600;
  --font-weight-900: 900;
}

@layer components {
  header {
    @apply w-full flex items-center justify-between px-3 md:px-10 lg:px-96 py-5 bg-white shadow-md;

    a > div {
      @apply font-mont font-600 text-xl md:text-2xl flex gap-2 items-center;
    }

    nav {
      @apply space-x-3 hidden md:flex md:items-center;
    }

    button {
      @apply p-1.5 text-sm bg-black text-white rounded-sm flex items-center gap-1;
    }
  }
}
